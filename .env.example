# MongoDB Atlas
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=rootx_shortener

# Server-only session signing secret (32+ random characters recommended)
JWT_SECRET=replace-with-a-long-random-random-secret
